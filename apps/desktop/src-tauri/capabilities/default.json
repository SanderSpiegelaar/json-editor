{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Edit files explicitly selected through the native open and save dialogs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-confirm",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file"
  ]
}
